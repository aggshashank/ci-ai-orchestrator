# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Kafka
kafka-python-ng>=2.2.0

# LangGraph + LangChain (versions must be mutually compatible — let pip resolve)
langgraph>=0.1.19
langchain>=0.2.6
langchain-community>=0.2.6
langchain-core>=0.2.22

# RAG
qdrant-client>=1.9.1
pypdf>=4.2.0
tiktoken>=0.7.0

# Config + utilities
pydantic>=2.7.3
pydantic-settings>=2.3.1
python-dotenv>=1.0.1
structlog>=24.2.0

# Prometheus metrics
prometheus-client>=0.20.0

# HTTP client (used by health check)
httpx>=0.27.0

# LLM providers (install only what you need; langchain-ollama is required for default)
langchain-ollama>=0.1.0
langchain-groq>=0.1.0
langchain-openai>=0.1.0

# PostgreSQL (async ORM + migrations)
sqlalchemy[asyncio]>=2.0.0
alembic>=1.13.0
asyncpg>=0.29.0

lz4>=4.3.2

# Rules engine
pyyaml>=6.0.1
jsonschema>=4.22.0
redis[hiredis]>=5.0.0
